{
  "installCommand": "pnpm install",
  "buildCommand": "mkdir -p api && echo 'const h=\"<h1>WeWe RSS</h1><p>API已启动</p>\";module.exports=(req,res)=>{if(req.url===\"/\"){res.setHeader(\"Content-Type\",\"text/html\");res.end(h)}else{res.json({ok:1})}}' > api/index.js",
  "outputDirectory": ".",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index" }
  ],
  "framework": null,
  "env": {
    "NODE_ENV": "production"
  }
}
