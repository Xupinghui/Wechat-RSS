{
  "installCommand": "pnpm install",
  "buildCommand": "mkdir -p api && echo 'export default function handler(req, res) { res.json({status:\"ok\"}) }' > api/index.js && mkdir -p client/dash && echo '<h1>WeWe RSS</h1>' > client/dash/index.html",
  "outputDirectory": ".",
  "rewrites": [
    { "source": "/dash/(.*)", "destination": "/client/dash/$1" },
    { "source": "/(.*)", "destination": "/api/index" }
  ],
  "framework": null,
  "env": {
    "NODE_ENV": "production"
  }
}
